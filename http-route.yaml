apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bug-route
  namespace: default
spec:
  parentRefs:
    - name: bug-gateway
  rules:
    # FITNESS TRACKER UI
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: fitness-service
          port: 80
